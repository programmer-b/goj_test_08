import 'package:flutter/material.dart';

class LoadComp extends StatefulWidget {
  const LoadComp({super.key});

  @override
  State<LoadComp> createState() => _LoadCompState();
}

class _LoadCompState extends State<LoadComp> {
  @override
  Widget build(BuildContext context) {
    return Column(
      mainAxisAlignment: MainAxisAlignment.center,
    );
  }
}
